{% extends 'base.html.twig' %}

{% block title %}Category{% endblock title %}


{% block main %}

    <main>

        <section class="row justify-around" id="category">
            <div style="background-color: {{ category.color}}"  class="card">
                <h1>{{ category.title }}</h1>
                <span class="row justify-center"><strong>Date de création : </strong>{{ category.creationDate |date("m/d/Y h:m:s") }}</span><br>
                <span class="row justify-center"><strong>Date de publication : </strong>{{ category.publicationDate |date("m/d/Y h:m:s") }}</span><br>
                <p>{{ category.color}}</p>
            </div>
        </section>
    </main>

{% endblock main %}